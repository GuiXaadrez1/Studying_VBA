Attribute VB_Name = "Módulo3"
Option Explicit

Sub objectWorkbook()

' Agora vamos trabalhar com os objetos do Woorkbook

' O objeto Workbook representa uma pasta de trabalho do Microsoft Excel. O objeto pasta de
' trabalho é um membro da coleção Workbooks. A coleção de pastas de trabalho contém todos
' os objetos Workbook atualmente abertos no Microsoft Excel.

'Workbook.Add ' Método que permite criar uma nova pasta de trabalho vazia e adcioná-la á coleção

' ActiveWorkbook.Close True 'Fecha a pasta de trabalho ativa. O método close tem alguns parâmetros,
' ele verifica se a propriedade Saved do objeto Workbook é true ou false. Se for true
' fechará a pasta e salvará, caso contrário, fechará a pasta sem salvar.

End Sub
